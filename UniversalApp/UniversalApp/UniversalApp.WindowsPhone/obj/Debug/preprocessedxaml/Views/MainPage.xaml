<base:PageBase x:Name="page" x:Class="UniversalApp.Views.MainPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:UniversalApp" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:base="using:UniversalApp.Views.Base" xmlns:i="using:Microsoft.Xaml.Interactivity" xmlns:core="using:Microsoft.Xaml.Interactions.Core" xmlns:converters="using:UniversalApp.Converters" DataContext="{Binding MainPageViewModel, Source={StaticResource Locator}}" mc:Ignorable="d">

    <Page.Resources>
    </Page.Resources>

    <i:Interaction.Behaviors>
    </i:Interaction.Behaviors>

    <Page.BottomAppBar>
        <CommandBar>
            <!--<AppBarButton x:Uid="bLogin" Label="Log in" Command="{Binding LoginCommand}" Visibility="{Binding Logged, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                <AppBarButton.Icon>
                    <PathIcon Data="{StaticResource IconLogin}" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5"/>
                </AppBarButton.Icon>
            </AppBarButton>-->

        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{StaticResource BackgroundBrush}">

        <ProgressBar x:Name="progressBar" IsIndeterminate="{Binding IsBusy}" />

        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Margin="10">
                <TextBlock x:Name="oTitle" x:Uid="oTitle" Grid.Row="0" Grid.Column="0" Text="Kaotiña's Store" Style="{StaticResource TitleTextBlockStyle}" Foreground="{StaticResource TitleBrush}" HorizontalAlignment="Left" VerticalAlignment="Center" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">

                    <TextBlock x:Uid="oLoged" Text="{Binding CurrentUserText}" Style="{StaticResource HeaderTextBlockStyle}" VerticalAlignment="Center" Margin="0,0,4,0" ToolTipService.ToolTip="logged as" />

                    <HyperlinkButton Grid.Row="0" Grid.Column="0" x:Uid="bLogin" ToolTipService.ToolTip="log in" Command="{Binding LoginCommand}">
                        <Path Data="{StaticResource IconLogin}" Stretch="Uniform" Fill="{StaticResource BorderBrush}">
                        </Path>
                    </HyperlinkButton>
                </StackPanel>

            </Grid>


        </Grid>

    </Grid>
</base:PageBase>