<base:PageBase x:Name="page" x:Class="UniversalApp.Views.MainPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:UniversalApp" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:base="using:UniversalApp.Views.Base" xmlns:i="using:Microsoft.Xaml.Interactivity" xmlns:core="using:Microsoft.Xaml.Interactions.Core" xmlns:media="using:Microsoft.Xaml.Interactions.Media" xmlns:converters="using:UniversalApp.Converters" DataContext="{Binding MainPageViewModel, Source={StaticResource Locator}}" mc:Ignorable="d">

    <Page.Resources>
        <local:Globals x:Key="AppGlobals" />

        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter" />
        
    </Page.Resources>

    <i:Interaction.Behaviors>
    </i:Interaction.Behaviors>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton x:Uid="bLogin" Label="Log in" Icon="Account" Command="{Binding LoginCommand}" Visibility="{Binding Logged, Converter={StaticResource BoolToVisibilityConverter}}" />
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{StaticResource BackgroundBrush}">

        <ProgressBar x:Name="progressBar" IsIndeterminate="{Binding IsBusy}" Background="Black" />

        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="6*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="5*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="oTitle" x:Uid="oTitle" Grid.Row="1" Grid.Column="1" Grid.RowSpan="1" Text="Kaotiña's Store" Style="{StaticResource TitleTextBlockStyle}" Foreground="{StaticResource TitleBrush}" />

        </Grid>

    </Grid>
</base:PageBase>