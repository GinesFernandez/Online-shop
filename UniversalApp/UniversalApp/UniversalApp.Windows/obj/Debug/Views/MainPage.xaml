<base:PageBase x:Name="page" x:Class="UniversalApp.Views.MainPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="using:UniversalApp" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:base="using:UniversalApp.Views.Base" xmlns:i="using:Microsoft.Xaml.Interactivity" xmlns:core="using:Microsoft.Xaml.Interactions.Core" xmlns:converters="using:UniversalApp.Converters" xmlns:telerik="using:Telerik.UI.Xaml.Controls.Data" xmlns:telerikData="using:Telerik.Data.Core" xmlns:listview="using:Telerik.UI.Xaml.Controls.Data.ListView" xmlns:listViewPrimitives="using:Telerik.UI.Xaml.Controls.Data.ListView.Primitives" DataContext="{Binding MainPageViewModel, Source={StaticResource Locator}}" mc:Ignorable="d">

    <Page.Resources>

        <Thickness x:Key="FormMargin">5,5,5,5</Thickness>

        <LinearGradientBrush x:Key="GradientBrush" EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Transparent" Offset="0" />
            <GradientStop Color="{StaticResource ThemeColor3}" Offset="1" />
        </LinearGradientBrush>

        <DataTemplate x:Key="WrapItemTemplate">
            <Grid>
                <Image Source="{Binding SmallPicture}" MaxHeight="215" MaxWidth="290" Stretch="Uniform" VerticalAlignment="Stretch" Margin="5" />
                <StackPanel Background="{StaticResource GradientBrush}" Height="60" VerticalAlignment="Bottom">
                    <TextBlock Margin="20,5,20,3" Text="{Binding Name}" TextTrimming="WordEllipsis" TextWrapping="WrapWholeWords" Height="25" Foreground="{StaticResource TextBrush2}" />
                    <TextBlock Margin="20 0" VerticalAlignment="Top" Text="{Binding Price, Converter={StaticResource TextCurrencyConverter}}" TextTrimming="WordEllipsis" MaxHeight="25" Foreground="{StaticResource TextBrush2}">
                    </TextBlock>
                </StackPanel>
                <Rectangle Height="4" VerticalAlignment="Bottom" Fill="{StaticResource BorderBrush}" />
            </Grid>
        </DataTemplate>

        <Style TargetType="listview:RadListViewItem">
            <Setter Property="Margin" Value="1 1 0 1" />
            <Setter Property="Padding" Value="0" />
        </Style>

    </Page.Resources>

    <i:Interaction.Behaviors>
    </i:Interaction.Behaviors>

    <Page.BottomAppBar>
        <CommandBar>

        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{StaticResource BackgroundBrush}">

        <ProgressBar x:Name="progressBar" IsIndeterminate="{Binding IsBusy}" />

        <!--Details-->
        <Border Visibility="{Binding IsDetailVisible, Converter={StaticResource BoolToVisibilityConverter}}" Canvas.ZIndex="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="50" BorderThickness="2" CornerRadius="3" Background="{StaticResource ControlBrush2}" BorderBrush="{StaticResource BorderBrush2}">
            <Grid>

                <Image Source="{Binding SelectedProduct.Picture}" MinHeight="215" MinWidth="290" Stretch="Uniform" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,0,0,50" />

                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="{StaticResource FormMargin}">
                    <HyperlinkButton x:Uid="bCloseDetail" ToolTipService.ToolTip="log in" Command="{Binding AddToCartCommand}">
                        <Path Data="{StaticResource IconCartAdd}" Stretch="Uniform" Fill="{StaticResource BorderBrush}">
                        </Path>
                    </HyperlinkButton>
                    <HyperlinkButton x:Uid="bCloseDetail" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="{StaticResource FormMargin}" ToolTipService.ToolTip="close" Command="{Binding CloseDetailCommand}">
                        <Path Data="{StaticResource IconCancel}" Stretch="Uniform" Fill="{StaticResource BorderBrush}">
                        </Path>
                    </HyperlinkButton>
                </StackPanel>

                <HyperlinkButton x:Uid="bNextDetail" Margin="{StaticResource FormMargin}" HorizontalAlignment="Right" VerticalAlignment="Center" ToolTipService.ToolTip="next" Command="{Binding NextDetailCommand}">
                    <Path Data="{StaticResource IconRightArrow}" Stretch="Uniform" Fill="{StaticResource BorderBrush}">
                    </Path>
                </HyperlinkButton>
                <HyperlinkButton x:Uid="bPreviousDetail" Margin="{StaticResource FormMargin}" HorizontalAlignment="Left" VerticalAlignment="Center" ToolTipService.ToolTip="prevoius" Command="{Binding PreviousDetailCommand}">
                    <Path Data="{StaticResource IconLeftArrow}" Stretch="Uniform" Fill="{StaticResource BorderBrush}">
                    </Path>
                </HyperlinkButton>

                <StackPanel Canvas.ZIndex="11" Background="{StaticResource BorderBrush2}" MinHeight="60" VerticalAlignment="Bottom">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding SelectedProduct.Name}" TextTrimming="WordEllipsis" TextWrapping="WrapWholeWords" Height="25" FontWeight="Bold" Margin="{StaticResource FormMargin}" Foreground="{StaticResource TextBrush4}" />
                        <TextBlock Text="{Binding SelectedProduct.Price, Converter={StaticResource TextCurrencyConverter}}" FontWeight="Bold" VerticalAlignment="Top" Margin="{StaticResource FormMargin}" Foreground="{StaticResource TextBrush4}" />
                    </StackPanel>
                    <TextBlock Margin="{StaticResource FormMargin}" VerticalAlignment="Top" Text="{Binding SelectedProduct.Description}" Foreground="{StaticResource TextBrush4}" TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--title-->
            <Grid Grid.Row="0" Margin="0,5,0,0">

                <TextBlock x:Name="oTitle" x:Uid="oTitle" Grid.Row="0" Grid.Column="0" Text="Kaotiña's Store" Style="{StaticResource TitleTextBlockStyle}" Foreground="{StaticResource TitleBrush}" HorizontalAlignment="Left" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">

                    <TextBlock x:Uid="oLoged" Text="{Binding CurrentUserText}" Style="{StaticResource HeaderTextBlockStyle}" Margin="0,0,4,0" ToolTipService.ToolTip="logged as" />

                    <HyperlinkButton Grid.Row="0" Grid.Column="0" x:Uid="bLogin" Margin="0,0,0,4" ToolTipService.ToolTip="log in" Command="{Binding LoginCommand}">
                        <Path Data="{StaticResource IconLogin}" Stretch="Uniform" Fill="{StaticResource BorderBrush}">
                        </Path>
                    </HyperlinkButton>
                </StackPanel>
            </Grid>

            <!--products-->
            <telerik:RadListView x:Name="listViewProducts" Grid.Row="1" Margin="0 5 0 0" Orientation="Vertical" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" ItemsSource="{Binding CollectionProducts}" ItemTemplate="{StaticResource WrapItemTemplate}" SelectedItem="{Binding SelectedProduct, Mode=TwoWay}">
                <telerik:RadListView.LayoutDefinition>
                    <listview:WrapLayoutDefinition ItemWidth="300" />
                </telerik:RadListView.LayoutDefinition>
            </telerik:RadListView>

        </Grid>

    </Grid>
</base:PageBase>

